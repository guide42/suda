language: php

php:
 - 7.1
 - hhvm

before_script:
 - composer self-update
 - composer install
 - mkdir -p build/logs

script:
 - phpunit --coverage-clover build/logs/clover.xml
 - php vendor/bin/coveralls -v

